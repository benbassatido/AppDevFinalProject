<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/loginRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_login_gradient"
    tools:context=".ui.login.LoginActivity">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="JOIN THE SQUAD"
        android:textColor="@color/white"
        android:textSize="@dimen/home_txt_size"
        android:textStyle="bold"
        android:letterSpacing="0.08"
        android:layout_marginBottom="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tilEmail"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"/>

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilEmail"
        android:theme="@style/MyTextInputThemeOverlay"
        style="@style/Widget.App.LoginTextInputLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/home_margin"
        android:layout_marginEnd="@dimen/home_margin"
        app:boxBackgroundMode="outline"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintBottom_toTopOf="@id/tilPassword"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etEmail"
            android:layout_width="match_parent"
            android:layout_height="@dimen/txt_height"
            android:hint="EMAIL / USERNAME"
            android:inputType="textEmailAddress"
            android:textColor="#AAAAAA"
            android:textColorHint="#AAAAAA"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:background="@null"/>
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilPassword"
        android:theme="@style/MyTextInputThemeOverlay"
        style="@style/Widget.App.LoginTextInputLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/home_margin"
        android:layout_marginEnd="@dimen/home_margin"
        android:layout_marginTop="@dimen/login_margin_padding"
        app:boxBackgroundMode="outline"
        app:layout_constraintTop_toBottomOf="@id/tilEmail"
        app:layout_constraintBottom_toTopOf="@id/btnLogin"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etPassword"
            android:layout_width="match_parent"
            android:layout_height="@dimen/txt_height"
            android:hint="PASSWORD"
            android:inputType="textPassword"
            android:textColor="@color/white"
            android:textColorHint="@color/sky_blue"
            android:paddingStart="@dimen/login_margin_padding"
            android:paddingEnd="@dimen/login_margin_padding"
            android:background="@null"/>
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLogin"
        android:layout_width="0dp"
        android:layout_height="@dimen/txt_height"
        android:layout_marginStart="@dimen/home_margin"
        android:layout_marginEnd="@dimen/home_margin"
        android:layout_marginTop="@dimen/home_margin"
        android:text="LOGIN"
        android:textAllCaps="true"
        android:textStyle="bold"
        android:letterSpacing="0.08"
        android:textColor="@color/white"
        app:cornerRadius="10dp"
        app:backgroundTint="@color/sky_blue"
        app:layout_constraintTop_toBottomOf="@id/tilPassword"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tvCreateAccount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create new account"
        android:textColor="@color/sky_blue"
        android:textSize="@dimen/home_txt_size"
        android:layout_marginTop="@dimen/home_margin"
        android:clickable="true"
        android:focusable="true"
        android:paddingTop="@dimen/login_margin_padding"
        android:paddingBottom="@dimen/login_margin_padding"
        android:paddingStart="@dimen/home_margin"
        android:paddingEnd="@dimen/home_margin"
        app:layout_constraintTop_toBottomOf="@id/btnLogin"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />




</androidx.constraintlayout.widget.ConstraintLayout>
